# Git hooks and shell scripts must stay LF so they run under Git Bash on Windows.
# A CRLF in a script's shebang breaks execution ("bad interpreter: /bin/bash^M").
.githooks/** text eol=lf
*.sh text eol=lf
